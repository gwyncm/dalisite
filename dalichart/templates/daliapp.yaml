apiVersion: v1
kind: Service
metadata:
  name: {{ include "dalichart.fullname" . }}-daliapp
  labels:
    io.kompose.service: daliapp
  {{- include "dalichart.labels" . | nindent 4 }}
  annotations:
    kompose.cmd: ../kompose convert -f ../daliserv/docker-compose.yml
    kompose.version: 1.31.2 (a92241f79)
spec:
  type: {{ .Values.daliapp.type }}
  selector:
    io.kompose.service: daliapp
  {{- include "dalichart.selectorLabels" . | nindent 4 }}
  ports:
	{{- .Values.daliapp.ports | toYaml | nindent 2 -}}